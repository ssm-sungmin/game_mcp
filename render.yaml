services:
  - type: web
    name: steam-game-mcp
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: MCP_TRANSPORT
        value: streamable-http
      - key: PORT
        value: 8000
